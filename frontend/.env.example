# SSA Intelligence Frontend - Environment Variables

# Backend API URL (optional - defaults to proxy)
# VITE_API_BASE_URL=http://localhost:3000/api

# Note: The vite.config.ts proxy already handles routing /api requests to the backend
# You only need to set VITE_API_BASE_URL if deploying frontend and backend separately

# Logo.dev Token (optional fallback when /api/config is unavailable)
# VITE_LOGO_DEV_TOKEN="pk_..."
